{
  "name": "hotelhub-monolith",
  "version": "1.0.0",
  "scripts": {
    "install-all": "npm install && npm install --prefix frontend && npm install --prefix Server",
    "build": "npm run install-all && npm run build --prefix frontend",
    "start": "node Server/app.js"
  }
}
